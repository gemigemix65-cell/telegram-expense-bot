# ----------------------------------------
# ۱. تعیین تصویر پایه
# ----------------------------------------
# استفاده از python:3.10-slim-buster
FROM python:3.10-slim-buster

# ----------------------------------------
# ۲. تنظیم دایرکتوری کاری درون کانتینر
# ----------------------------------------
WORKDIR /app

# ----------------------------------------
# ۳. نصب پیش‌نیازهای سیستمی (FFmpeg) و پاکسازی
# ----------------------------------------
# استفاده از یک دستور RUN واحد برای به‌روزرسانی، نصب و پاکسازی
RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        # نصب پکیج حیاتی FFmpeg برای پردازش ویس
        ffmpeg \
        # نصب پکیج‌های توسعه برای زمانی که برخی وابستگی‌های پایتون نیاز به کامپایل دارند
        build-essential \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# ----------------------------------------
# ۴. کپی کردن فایل‌ها و نصب وابستگی‌ها
# ----------------------------------------
# فایل requirements.txt را کپی می‌کنیم.
COPY requirements.txt .

# نصب وابستگی‌های پایتون
# توجه: اگر برخی پکیج‌ها (مانند SpeechRecognition) نیاز به کامپایل با build-essential داشته باشند، 
# این دستور نصب را پس از نصب بسته‌های سیستمی اجرا می‌کند.
RUN pip install --no-cache-dir -r requirements.txt

# ----------------------------------------
# ۵. حذف بسته‌های توسعه برای کوچک‌سازی ایمیج نهایی
# ----------------------------------------
# حذف build-essential و بسته‌های اضافه پس از نصب پکیج‌های پایتون 
# (چون دیگر نیازی به کامپایل نداریم).
RUN apt-get purge -y --auto-remove build-essential \
    && rm -rf /var/lib/apt/lists/*

# ----------------------------------------
# ۶. کپی کردن فایل‌های کد اصلی
# ----------------------------------------
COPY main.py .

# ----------------------------------------
# ۷. اجرای ربات
# ----------------------------------------
# دستور اصلی اجرای برنامه
CMD ["python", "main.py"]
